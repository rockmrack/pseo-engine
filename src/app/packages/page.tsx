// PACKAGES INDEX PAGE
import { Metadata } from 'next';
import Link from 'next/link';
import { maintenancePackages } from '@/lib/data/packages-database';
import { Breadcrumb } from '@/components/navigation/Breadcrumb';
import { CTASection } from '@/components/pseo';
export const metadata: Metadata = { title: 'Maintenance Packages | Hampstead Renovations', description: 'Monthly boiler cover and maintenance packages for London homes.', alternates: { canonical: 'https://hampsteadrenovations.co.uk/packages' } };
export default function PackagesIndexPage() { const bc = [{ name: 'Home', href: '/' }, { name: 'Packages', href: '/packages', current: true }]; return (<main className="min-h-screen"><section className="bg-gradient-to-br from-emerald-700 to-slate-800 text-white py-16"><div className="container mx-auto px-4"><Breadcrumb items={bc} className="mb-6" /><h1 className="text-4xl font-bold mb-4">Maintenance Packages</h1><p className="text-xl text-emerald-100 mb-6">Choose from our range of boiler cover and home maintenance packages. Protect your home with affordable monthly plans.</p></div></section><section className="py-16 bg-white"><div className="container mx-auto px-4"><h2 className="text-3xl font-bold text-slate-900 mb-8">Our Packages</h2><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">{maintenancePackages.map((pkg) => <Link key={pkg.slug} href={'/packages/' + pkg.slug} className="group bg-slate-50 rounded-2xl p-8 hover:shadow-lg transition-all border border-slate-200"><span className="inline-block bg-emerald-100 text-emerald-700 px-3 py-1 rounded-full text-xs font-medium mb-4">{pkg.tier.toUpperCase()}</span><h3 className="text-2xl font-bold text-slate-900 group-hover:text-emerald-600 mb-2">{pkg.name}</h3><p className="text-slate-600 text-sm mb-4">{pkg.tagline}</p><div className="flex items-baseline gap-2 mb-4"><span className="text-3xl font-bold text-emerald-600">L{pkg.monthlyPrice}</span><span className="text-slate-500">/month</span></div><ul className="space-y-2 mb-6">{pkg.coverage.slice(0, 3).map((c, i) => <li key={i} className="text-sm text-slate-600 flex items-start gap-2"><span className="text-emerald-500">*</span>{c}</li>)}</ul><span className="text-emerald-600 font-semibold group-hover:underline">View Details &gt;</span></Link>)}</div></div></section><CTASection cta={{ headline: 'Not Sure Which Package?', subtext: 'Get a free consultation to find the right cover for your home.', buttonText: 'Get Advice', phoneNumber: '020 8000 0000' }} /></main>); }