// LANDLORDS INDEX PAGE
import { Metadata } from 'next';
import Link from 'next/link';
import { landlordServices } from '@/lib/data/landlord-database';
import { Breadcrumb } from '@/components/navigation/Breadcrumb';
import { CTASection } from '@/components/pseo';
export const metadata: Metadata = { title: 'Landlord Services | Hampstead Renovations', description: 'Comprehensive services for landlords and property managers in London.', alternates: { canonical: 'https://hampsteadrenovations.co.uk/landlords' } };
export default function LandlordsIndexPage() { const bc = [{ name: 'Home', href: '/' }, { name: 'Landlord Services', href: '/landlords', current: true }]; return (<main className="min-h-screen"><section className="bg-gradient-to-br from-blue-900 to-slate-800 text-white py-16"><div className="container mx-auto px-4"><Breadcrumb items={bc} className="mb-6 text-blue-200" /><h1 className="text-4xl font-bold mb-4">Landlord Services</h1><p className="text-xl text-blue-100 mb-6">Stay compliant with all regulations. We provide Gas Safety Certificates, EICR reports, and ongoing property maintenance across London.</p></div></section><section className="py-16 bg-white"><div className="container mx-auto px-4"><h2 className="text-3xl font-bold text-slate-900 mb-8">Our Services for Landlords</h2><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">{landlordServices.map((s) => <Link key={s.slug} href={'/landlords/' + s.slug} className="group bg-slate-50 rounded-xl p-6 hover:shadow-lg hover:bg-white transition-all border border-slate-200"><h3 className="text-xl font-bold text-slate-900 group-hover:text-blue-600 mb-3">{s.name}</h3><p className="text-slate-600 text-sm mb-4 line-clamp-2">{s.description}</p><div className="flex items-center justify-between"><span className="text-blue-600 font-semibold">From L{s.price.from} {s.price.unit}</span></div></Link>)}</div></div></section><CTASection cta={{ headline: 'Need Help Managing Your Property?', subtext: 'Professional services for London landlords.', buttonText: 'Get Free Quote', phoneNumber: '020 8000 0000' }} /></main>); }